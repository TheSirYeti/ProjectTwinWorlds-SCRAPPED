using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SinTable : MonoBehaviour
{
    //Creo la variable LookUpTable donde asigno T1 va a ser int,y T2 va a ser un float
    float t1;
    float t2;

    //Le doy referencia a la variable de LookUpTable
    /*...*/

    /// <summary>
    /// Tomando un valor,devuelvo el angulo Sin de ese valor en radians
    /// </summary>
    /// <param name="value"></param>
    /// <returns></returns>
    /*...*/


    //void Update()
    //{
    //    if (Input.GetKeyUp(KeyCode.Alpha1))
    //        Debug.Log(_sinTable.ReturnValue(1));//Le Paso a la Table el valor 1,si lo tiene me lo devuelve,sino lo crea y me lo devuelve
    //    if (Input.GetKeyUp(KeyCode.Alpha2))
    //        Debug.Log(_sinTable.ReturnValue(2));//Le Paso a la Table el valor 2,si lo tiene me lo devuelve,sino lo crea y me lo devuelve
    //    if (Input.GetKeyUp(KeyCode.Alpha3))
    //        Debug.Log(_sinTable.ReturnValue(3));//Le Paso a la Table el valor 3,si lo tiene me lo devuelve,sino lo crea y me lo devuelve
    //    if (Input.GetKeyUp(KeyCode.Alpha4))
    //        Debug.Log(_sinTable.ReturnValue(4));//Le Paso a la Table el valor 4,si lo tiene me lo devuelve,sino lo crea y me lo devuelve
    //}
}
