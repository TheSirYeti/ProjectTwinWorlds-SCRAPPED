
using UnityEngine;

public class RopeCollision : MonoBehaviour
{
    /*public CollisionType currentCollision;
    private void OnTriggerEnter(Collider other)
    {

        if (other.gameObject.layer == LayerMask.NameToLayer("Movable Object"))
        {
            currentCollision = CollisionType.MOVABLE_OBJECT;
        }
        
        else if (other.gameObject.layer == LayerMask.NameToLayer("Breakable Object"))
        {
            currentCollision = CollisionType.BREAKABLE_OBJECT;
        }
        
        else if (other.gameObject.layer == LayerMask.NameToLayer("Pentadente"))
        {
            currentCollision = CollisionType.PENTADENTE;
        }
        
        else if (other.gameObject.layer == LayerMask.NameToLayer("Grab Spot"))
        {
            currentCollision = CollisionType.GRAB_SPOT;
        }
        else if (other.gameObject.layer == LayerMask.NameToLayer("Wall"))
        {
            currentCollision = CollisionType.WALL;
        }
        else currentCollision = CollisionType.NONE;
    }

    private void OnTriggerExit(Collider other)
    {
        currentCollision = CollisionType.NONE;
    }

    public enum CollisionType
    {
        WALL,
        MOVABLE_OBJECT,
        BREAKABLE_OBJECT,
        PENTADENTE,
        GRAB_SPOT,
        NONE
        
    }*/
}
